# Pages static output (for `wrangler pages deploy dist` only).
# Do NOT run `wrangler deploy` from repo root — the API lives in worker/wrangler.toml.
name = "zwo-builder"
pages_build_output_dir = "dist"
compatibility_date = "2025-01-01"
